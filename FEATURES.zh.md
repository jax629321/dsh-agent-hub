# 智能体协作台(dsh-agent-hub)· 功能特色介绍

> 一个群,就是一个可以自主运转、持续交付的 AI 协作团队。
> 群聊不只是聊天,而是**自主决策、任务分发、执行验证、迭代优化**的协作总线。

---

## 一、产品定位:一个群 = 一个可自主运转的协作流程

传统多 Agent 对话是"你来我往地聊";智能体协作台把协作组织成了**一个群聊房间**:

- 群里的每个成员是一个独立的 Agent(接入 OpenAI 兼容 API 即可,可混合不同模型);
- 群里有明确的角色分工:**人类 Owner(需求与验收的最终裁判)→ 统筹大脑(唯一的规划决策中枢)→ 执行成员(单一职责的执行者)**;
- 群里所有的工作流(拆解、派单、执行、验收、沉淀)都发生在群消息流上,**过程全程可见、可追溯、可随时介入**。

你把目标丢进群里,剩下的推进由群自己完成——这正是"长任务自主运转"的产品形态。

---

## 二、群聊即总线:如何用"群"实现自主决策

### 2.1 消息即指令,群聊即状态机

- **`@成员名` 定向派单**:在输入框 `@成员` + 发送,任务即时投递给该成员,@ 支持输入时自动弹出成员列表一键选择;
- **普通消息全员可见**:用于信息同步、讨论、补充上下文;
- **每一条消息都流入群的历史**,成为大脑下一轮决策的依据——群聊本身就是协作的"运行日志"。

### 2.2 统筹大脑:群里的决策中枢

把任意一名成员「设为大脑」后,大脑接管统筹:

- **四问决策**:每轮决策前显式回答"目标?现状?可用成员与能力?完成定义(DoD)?",缺项先澄清,不臆测;
- **结构化输出**:每轮只输出一个 JSON 决策(`plan` 思路 / `dispatch` 分派清单 / `status` 状态 / `memory` 记忆沉淀),杜绝空泛闲聊;
- **区分事实与推断**:引用群聊、记忆、成员回复时注明依据,抑制幻觉与范围蔓延。

### 2.3 全局协作章程:约束所有成员的"宪法"

插件内置一套可热编辑的**全局协作章程**(System Charter),随每次派单与每轮决策前置注入给大脑和所有成员:

- 角色职责边界(谁决策、谁执行、谁验收);
- 任务必须原子化、SMART(具体/可衡量/可达成/相关/有时限);
- 验收以"可复现"为最终判据;
- 阻塞立即上报、不静默停滞、不越权、不曲解范围。

### 2.4 按轮次自主推进

大脑按 **决策 → 分派 → 收齐 → 验收 → 沉淀 → 下一轮** 的状态机循环推进:

- 勾选「自动批准持续任务」后,**授权范围内无需人工逐轮确认**,大脑自主策动任务一轮接一轮,直到交付完成或触及熔断条件;
- 每轮计划默认仍可人工把关(逐轮「确认执行/否决」),人类保留最高介入权:随时「叫停」、修改计划、否决。

---

## 三、多 Agent 协作:任务分发与执行

### 3.1 原子任务拆分

大脑不会把模糊的大需求直接丢给成员,而是拆成:

> 一个成员 = 一个原子任务 = 一个明确交付物 + 一条验收标准 + 一句可执行指令

依赖任务标注前置条件、按序派发;互不依赖的任务**并行分发**,最大化吞吐。

### 3.2 能力匹配分工

大脑按成员能力画像派活:

- 工程/代码/文件类 → 交给具备执行能力的成员;
- 特定模型/API 能力 → 匹配对应配置的成员;
- 同一成员不过载,必要时串行化;能力不匹配时说明限制并调整,不硬派。

### 3.3 执行与回流

- 成员收到任务先复述理解,再动手;
- 交付必须是**完整成果**(结论 + 产物路径/全文 + 复现方式),禁止只回"收到";
- 执行结果实时回流到群消息流,成为大脑下一轮决策的输入;
- 阻塞时按"卡点 + 已尝试方案 + 需要的帮助"结构化上报,不静默停滞。

---

## 四、验证与返工:可复现的验收闭环

长任务最容易"交付了但没验收、验收了但不可复现"。本插件的验收不是一句"通过了",而是一个闭环:

1. **逐条核对**:大脑对每个交付逐条对照派单时的验收标准;
2. **可复现判据**:要求给出复现步骤/运行方式,以"能复现"为最终通过标准;
3. **一次性返工清单**:不合格时给出精确到点的可执行返工清单,不重复模糊批评,成员无需追问即可修改;
4. **通过即沉淀**:验收通过后,把结论、关键决策、产物位置、遗留风险写入**群记忆**,宣布本轮完成;
5. **回归防退化**:已通过但后续发现新问题,按新任务重新派发,不混入已收尾任务。

> 效果:每一轮交付都有明确结论、可追溯来源、可复现证据;返工有依据,迭代不空转。

---

## 五、自主优化:群记忆与多轮迭代(长任务场景)

### 5.1 群记忆 = 跨轮次的团队"长期记忆"

- 大脑每轮把关键事实、结论、交付物要点沉淀进群记忆;
- 此后每一轮决策都会把群记忆注入给大脑,作为既有上下文;
- 任务结束、甚至换新任务后,记忆依然复用——**群是一个有记忆、会成长的团队,而不是一次性对话**。

### 5.2 自主优化循环

对"从 0 到 1 的完整项目"这类长任务,插件支持**多轮自主迭代**:

```
需求澄清 → 方案设计 → 模块拆分 → 并行开发 → 集成联调
     → 逐项验收 → 返工修复 → 复验通过 → 记忆沉淀 → 下一阶段
```

- 每一轮都以上一轮的验收结果和群记忆为输入,持续逼近完成定义(DoD);
- 大脑每轮判断"是否离 DoD 更近",接近收敛即收尾,**不无限循环**;
- 内置熔断保护:最大轮次、单轮超时、人类叫停三重兜底,长任务也不会失控。

### 5.3 中途介入不影响流程

长任务进行中,人类随时可以:

- 在群聊里补充信息 / 调整方向(下一轮决策自动纳入);
- 对某轮计划「否决」并给出理由(大脑据此修正);
- 一键「叫停」正在运行的任务;
- 删除/归档房间、恢复历史,管理整个协作资产。

---

## 六、典型使用场景

| 场景 | 群里的协作方式 |
|---|---|
| **完整项目开发(0 → 1)** | 需求 → 大脑拆模块 → 多成员并行开发 → 集成 → 逐项验收 → 返工 → 交付 |
| **技术方案选型与实现** | 大脑组织方案对比 → 成员各自调研 → 交叉验证 → 定案 → 落地实现 |
| **代码审查与修复** | 成员产出代码 → 大脑按验收标准审查 → 一次性返工清单 → 复验 |
| **批量调研 / 文档撰写** | 并行分派调研 → 汇总交叉核对 → 成稿 → 按章节验收 |
| **持续优化迭代** | 基于群记忆与上一轮反馈,自主发起下一轮改进,直至达标 |

---

## 七、特性速览

- ✅ 群聊即协作总线:`@成员` 定向派单、输入时成员自动补全
- ✅ 统筹大脑:四问决策、JSON 结构化输出、按轮次自主推进
- ✅ 全局协作章程:角色边界、原子任务、验收规范,随插件分发、可热编辑
- ✅ 自动批准持续任务:授权后无需逐轮人工确认,自主策动
- ✅ 任务分发:原子化拆分、能力匹配、依赖排序、并行执行
- ✅ 验收闭环:可复现判据、一次性返工清单、通过即沉淀
- ✅ 群记忆:跨轮次 / 跨任务的长期团队记忆
- ✅ 长任务兜底:最大轮次、单轮超时、人类随时叫停/否决
- ✅ 内置后端:随插件分发,自动探测、自动装依赖、开箱即用
- ✅ 干净纯净:无外部服务依赖、无残留数据,全新用户零负担上手

---

> 智能体协作台 —— 把"一个群"变成一支可以自主决策、分工执行、验证优化、持续交付的 AI 团队。
